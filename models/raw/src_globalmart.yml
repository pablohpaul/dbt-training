version: 2

sources:
  - name: public
    description: a clone of the SF Database
    database: raw
    tables:
      - name: customers
      - name: orders
        description: raw orders table
        columns:
          - name: orderid
            description: primary key for orders
      - name: product